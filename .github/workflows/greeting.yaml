name: Greeting
on: push                #it will trigger the workflow on push event only
# on: workflow_dispatch
jobs:
    greet:
        runs-on: ubuntu-latest
        steps:
            - name: "greet user"
              run: echo "Hello world!"


